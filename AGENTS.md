# Research campaign

Own only this campaign while working here. Follow
`.agents/skills/research-session/SKILL.md`, `research/repository.md`, and
`research/reduction.md`. The fixed question is
`campaigns/hamiltonian-path-consecutive-sets/question.md`; the initial budget is
20 rounds. Write all artifacts in English. Commit independent Prepare before
constructing candidates. Retain failures and incremental commits. Use uv and a
committed lockfile. Prove polynomial forward and recovery maps for every valid
target output, including empty graphs and `NO-SOLUTION`.

Read shared experience in place at
`/Users/xiweipan/Codes/autoresearch-gadgets/research/experience/entries/`; do not
copy it. Do not depend on sibling research repositories at runtime. Preserve
unrelated edits. Do not publish, update the board, comment upstream or merge.

Use the registered `.codex/agents/research-reviewer.toml` with a fresh context
for independent review. The main researcher owns semantics, oracles and proof.
Narrow mechanical delegation is allowed only when useful; do not delegate a
complete campaign.

After completion or a justified stop, return to the parent session's assigned
Construction Open queue. Do not select a question outside that queue.
